# Nixpacks Configuration for Astro
# Ref: https://nixpacks.com/docs/configuration/file

[phases.setup]
nixPkgs = ["nodePackages.bun", "nodejs_22"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun run preview --host 0.0.0.0 --port ${PORT:-3000}"
